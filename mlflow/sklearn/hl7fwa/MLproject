name: hl7fwa-rose

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      num_estimators: float
      max_depth: {type: float, default: 15.0}
    command: "python train.py --num_estimators {num_estimators} --max_depth {max_depth}"
